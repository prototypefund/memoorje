{
  "name": "@memoorje/crypto",
  "description": "Encryption container formats based on the Web Cryptography API",
  "keywords": ["webcrypto", "container format"],
  "version": "0.1.0",
  "main": "dist/index.umd.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "devDependencies": {
    "@playwright/test": "^1.14.1",
    "typescript": "^4.4.3",
    "webpack-cli": "^4.8.0"
  },
  "scripts": {
    "serve:tests": "python3 -m http.server 3080 >/dev/null 2>&1",
    "build": "tsc && webpack --config webpack.config.js --mode production",
    "prepare": "npm run build",
    "test": "tsc && npx playwright test"
  }
}
